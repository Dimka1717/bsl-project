
&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	Если ЗначениеЗаполнено(Объект.Ссылка) Тогда
		
		Отказ = Истина;
		
		ВзаимодействиеКонтурМаркировкаКлиент.ОткрытьФормуТовара(Объект.Ссылка);
		
	КонецЕсли;
	
КонецПроцедуры
