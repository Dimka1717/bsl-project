
&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	Если ЗначениеЗаполнено(Объект.Ссылка) Тогда
		
		Отказ = Истина;
		
		ВзаимодействиеКонтурМаркировкаКлиент.ОткрытьФормуСертификата(Объект.Ссылка);
		
	КонецЕсли;
	
КонецПроцедуры
