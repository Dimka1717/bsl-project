
&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	Если ЗначениеЗаполнено(Объект.Ссылка) Тогда
		
		Отказ = Истина;
		
		ВзаимодействиеКонтурМаркировкаКлиент.ОткрытьФормуОперации(Объект.Ссылка);
		
	КонецЕсли;
	
КонецПроцедуры
