
Процедура ПередЗаписью(Отказ)
	
	Если ЗначениеЗаполнено(ИмяФайла) Тогда
		Наименование = ИмяФайла;
	ИначеЕсли ЗначениеЗаполнено(Идентификатор) Тогда
		Наименование = ТипФайла + " (" + Идентификатор + ")";
	Иначе
		Наименование = ТипФайла;
	КонецЕсли;
	
КонецПроцедуры
